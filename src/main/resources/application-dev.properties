# ===========================================================================
# DEV profile — local developer workstation.
#
# Activate with: SPRING_PROFILES_ACTIVE=dev
# (spring-boot-maven-plugin is configured to activate this automatically
# via `mvn spring-boot:run` — no env var needed locally.)
#
# The base application.properties already carries all dev-safe defaults,
# so this file only needs to set things that differ from the base.
# ===========================================================================

logging.level.org.springframework.security=INFO
logging.level.com.example.form8038cp=DEBUG
